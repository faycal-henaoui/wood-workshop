{
  "name": "workshop-app",
  "version": "1.0.0",
  "description": "Heroku deployment root",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install-all": "npm install --prefix back-end && npm install --prefix front-end",
    "heroku-postbuild": "cd front-end && npm install --include=dev && npm run build",
    "start": "node back-end/index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
